import { ArrowUpRight } from "lucide-react";
import { Card } from "@/components/ui/card";
import { PageIntro } from "../components/PageIntro";
import { participation } from "../data/content";

export default function CommunityPage(){return <main><PageIntro eyebrow="Community" title="There is useful work for every kind of contributor." description="Participate through code, design, research, writing, local service, or careful review. Contribution begins with a clearly scoped issue and ends with a visible result."/><section className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="divide-y divide-border border-y border-border">{participation.map(([title,body],index)=><div key={title} className="grid gap-4 py-7 md:grid-cols-[48px_200px_1fr_auto] md:items-center"><span className="font-mono text-xs text-muted-foreground">0{index+1}</span><h2 className="font-medium">{title}</h2><p className="text-sm leading-6 text-muted-foreground">{body}</p><ArrowUpRight className="size-4 text-primary"/></div>)}</div><Card className="mt-12 p-7"><p className="text-xs font-medium uppercase tracking-[.16em] text-primary">Participation policy</p><h2 className="mt-4 text-3xl font-medium tracking-tight">Contribution must be safe, attributable, and reviewable.</h2><p className="mt-5 max-w-2xl leading-7 text-muted-foreground">Version 1 uses GitHub issues as the canonical contribution entry point. Public community onboarding opens only after the Founder approves the launch and moderation controls.</p></Card></section></main>}
